package controller;

/**
 * This interface represents a controller for processing image to the designated intention of the
 * user based on their input.
 */
public interface ImageProcessingController {
  /**
   * Start the image processing program.
   */
  void start();
}
